apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: meteo-proxy
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: meteo-proxy
                port:
                  number: 80
