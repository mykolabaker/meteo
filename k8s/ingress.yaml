apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: meteo-proxy
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: meteo-proxy
                port:
                  number: 80
